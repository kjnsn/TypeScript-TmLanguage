// @onlyOwnGrammar - As this has jsx
const ProductPrice = ({inStock, price}) => {
  const weekDays = []
  return (
    return weekDays.filter((weekDay, index) => ({} || index < 3)).map((weekDay, key) => (
      <p key={key}>
        
      </p>
    ))
  )
}